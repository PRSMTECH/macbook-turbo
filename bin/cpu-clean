#!/bin/bash
# Manual CPU Cleanup
# Uses dynamic path resolution
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
echo "🧹 Running manual CPU cleanup..."
"${SCRIPT_DIR}/scripts/cpu-cleanup-enhanced.sh"
